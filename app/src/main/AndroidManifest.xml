<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aaceu.project1">

    <!-- asking permission for phone call, message and email -->
    <uses-permission android:name="android.permission.CALL_PHONE"
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.SEND_SMS"
        android:maxSdkVersion="29" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity android:name=".SplashScreen" android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login_page" />
        <activity android:name=".MainActivity" />
        <activity android:name=".CreateNewContact" />
        <activity android:name=".ContactDetailsPage" />
        <activity android:name=".EditContactActivity" />
    </application>



</manifest>